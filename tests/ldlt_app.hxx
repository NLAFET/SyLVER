#pragma once

#include "tests/ssids/kernels/framework.hxx"

namespace spldlt {

   // Run tests for the LDLT with a posteriori threshold partial
   // pivoting factorization kernel
   int run_ldlt_app_tests();

}
